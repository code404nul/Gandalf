# Gandalf

GPX reader -> interactive map -> cal estimation/altitude/distance in python(?)

Il est recommander de crée une venv python

```
python3 -m venv venv
./venv/bin/activate
``` 
**Sur linux uniquement**